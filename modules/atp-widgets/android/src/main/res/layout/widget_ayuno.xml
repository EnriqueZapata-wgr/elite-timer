<?xml version="1.0" encoding="utf-8"?>
<!--
  MB-32 · Widget de ayuno (2x2, solo lectura). El Chronometer tictaquea
  solo (sistema): presencia sin despertar a nadie. El reloj de arena va en
  el color de concepto ayuno #6B46C1 (el mismo en claro y oscuro).
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
  android:id="@+id/ayuno_root"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:orientation="vertical"
  android:background="@drawable/widget_bg_dark"
  android:paddingStart="14dp"
  android:paddingEnd="14dp"
  android:paddingTop="12dp"
  android:paddingBottom="12dp">

  <LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical">

    <ImageView
      android:layout_width="14dp"
      android:layout_height="14dp"
      android:layout_marginEnd="6dp"
      android:src="@drawable/ic_atp_reloj_arena"
      android:contentDescription="@string/widget_ayuno_label" />

    <TextView
      android:id="@+id/ayuno_header"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:fontFamily="@font/poppins_semibold"
      android:letterSpacing="0.12"
      android:textSize="10sp"
      android:maxLines="1"
      android:text="@string/widget_ayuno_header" />
  </LinearLayout>

  <TextView
    android:id="@+id/ayuno_vacio"
    android:layout_width="match_parent"
    android:layout_height="0dp"
    android:layout_weight="1"
    android:gravity="center"
    android:fontFamily="@font/poppins_regular"
    android:textSize="13sp"
    android:visibility="gone" />

  <Chronometer
    android:id="@+id/ayuno_contador"
    android:layout_width="wrap_content"
    android:layout_height="0dp"
    android:layout_weight="1"
    android:gravity="center_vertical"
    android:fontFamily="@font/poppins_semibold"
    android:textSize="30sp"
    android:maxLines="1" />

  <TextView
    android:id="@+id/ayuno_detalle"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:fontFamily="@font/poppins_regular"
    android:textSize="11sp"
    android:maxLines="1" />
</LinearLayout>
