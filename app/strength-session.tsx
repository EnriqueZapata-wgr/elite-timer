/**
 * Alias permanente de /session (Ola 2 Fitness PR1, ANEXO_B_FITNESS §5).
 * La ruta vieja sigue viva y rinde el mismo runner con los mismos params:
 * deep links, OTA viejos y stashes en vuelo no se rompen.
 */
export { default } from './session';
