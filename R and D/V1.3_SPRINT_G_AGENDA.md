# V1.3 Sprint G — AGENDA completa (backend + ruta + edición + push server-side)

**Para:** Claude Code · **Fecha objetivo:** 2026-06-28+ · **Branch:** `feat/agenda-v13g`
**Owner:** Enrique · **Reviewer:** Cowork audita branch antes del merge

---

## 1. Contexto y filosofía

HOY queda cerrado al 100%. Siguiente pantalla del flujo macro: **AGENDA**.

### Decisiones de criterio ya tomadas con Enrique

1. **Ubicación:** ventana dependiente de HOY — ruta `/agenda` accesible desde HOY (botón/card en cabecera), NO tab dedicada. Cuando abres /agenda, "saliste temporalmente" de HOY, no cambiaste de contexto. Atrás regresa a HOY.
2. **Generación:** auto desde protocolo + cronotipo + usuario sobreescribe lo que quiera (override item por item + crear custom).
3. **Notificaciones:** push **server-side completas** (Edge Function + tokens registro + remote scheduling). No solo locales.
4. **Visual:** mini-cards horizontales (imagen thumbnail B/N a la izq + texto + hora a la der). Reusa imágenes `assets/images/agenda/*` que YA existen (45 imgs / 12 categorías).

### Filosofía no negociable

- **Simple vence inteligente.** Formularios planos > drag-drop. Captura > experiencia.
- **Reusar lo que existe** antes de crear nuevo (EditDayModal, generateDailyPlan, custom_agenda_actions).
- **Defensivo:** datos faltantes caen a placeholder, no crashean.
- **Combinable con notifs + personalización + widgets** (widgets para V2.x — diseño data layer compatible).

---

## 2. Estado actual (verificado en código)

- `daily_plans` table con `actions JSONB` autogen por `generateDailyPlan()` (protocol-builder-service.ts:66-100). Persiste plan del día con scheduled_time, category, duration_min, instructions.
- `user_day_preferences.custom_agenda_actions` JSONB array para items manuales.
- `EditDayModal.tsx:64-150` — UI editor existente: agregar/quitar/reordenar (formulario plano, alineado simple>smart).
- `day-compiler.ts:buildAgenda()` une 6 fuentes (daily_plans, smartFastBreak, custom_agenda_actions, meals, sleep).
- `expo-notifications` v0.32.17 instalado. Solo se usa para badge visual (`notifications-service.ts`). NO push real.
- 45 imágenes B/N en `assets/images/agenda/{cardio,comida,despertar,entrenar,hidratacion,meditacion,off-pantallas,otros,sleep,sol-am,sol-pm,suplementos}/` (3-4 variantes c/u).

---

## 3. Tareas quirúrgicas

### 3.A — Backend SQL

Crear migración `supabase/migrations/098_agenda_events.sql` idempotente:

```sql
-- ============================================================
-- Migración 098: Agenda events (sistema CRUD + tracking + notificaciones)
-- ============================================================

-- TABLA 1: eventos plantilla del usuario (recurrentes o únicos)
CREATE TABLE IF NOT EXISTS agenda_events (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES profiles(id) ON DELETE CASCADE NOT NULL,
  name TEXT NOT NULL,
  time TIME NOT NULL,                          -- hora del día, ej. 07:00
  category TEXT NOT NULL,                      -- nutricion, fitness, sueño, mente, ritmo, etc.
  source TEXT NOT NULL DEFAULT 'manual',       -- 'manual' | 'protocol' | 'chronotype'
  duration_min INT,                            -- opcional
  notify_minutes_before INT DEFAULT 0,         -- 0 = sin notif; 5/10/15/30/60
  is_active BOOLEAN DEFAULT TRUE,              -- soft delete
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);

CREATE INDEX IF NOT EXISTS idx_agenda_events_user ON agenda_events(user_id, time);

ALTER TABLE agenda_events ENABLE ROW LEVEL SECURITY;
CREATE POLICY agenda_events_own ON agenda_events FOR ALL USING (auth.uid() = user_id);

-- TABLA 2: log de instancias diarias (tracking estado + scheduling notif)
CREATE TABLE IF NOT EXISTS agenda_event_logs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES profiles(id) ON DELETE CASCADE NOT NULL,
  event_id UUID REFERENCES agenda_events(id) ON DELETE CASCADE,
  date DATE NOT NULL,
  status TEXT NOT NULL DEFAULT 'pending',      -- pending | completed | skipped | snoozed
  scheduled_at TIMESTAMPTZ NOT NULL,           -- timestamp completo del evento
  notify_at TIMESTAMPTZ,                       -- cuándo disparar push (NULL = no notif)
  notified_at TIMESTAMPTZ,                     -- cuándo se envió la push (NULL = pendiente)
  completed_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT now()
);

CREATE UNIQUE INDEX IF NOT EXISTS uniq_agenda_log_per_day
  ON agenda_event_logs(user_id, event_id, date);
CREATE INDEX IF NOT EXISTS idx_agenda_logs_notify_pending
  ON agenda_event_logs(notify_at) WHERE notified_at IS NULL AND notify_at IS NOT NULL;

ALTER TABLE agenda_event_logs ENABLE ROW LEVEL SECURITY;
CREATE POLICY agenda_event_logs_own ON agenda_event_logs FOR ALL USING (auth.uid() = user_id);

-- TABLA 3: tokens push de los devices del usuario
CREATE TABLE IF NOT EXISTS user_notification_tokens (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES profiles(id) ON DELETE CASCADE NOT NULL,
  expo_push_token TEXT NOT NULL,
  platform TEXT NOT NULL,                      -- 'ios' | 'android'
  device_id TEXT,                              -- opcional, para múltiples devices
  last_used_at TIMESTAMPTZ DEFAULT now(),
  created_at TIMESTAMPTZ DEFAULT now()
);

CREATE UNIQUE INDEX IF NOT EXISTS uniq_user_token ON user_notification_tokens(user_id, expo_push_token);

ALTER TABLE user_notification_tokens ENABLE ROW LEVEL SECURITY;
CREATE POLICY user_notification_tokens_own ON user_notification_tokens FOR ALL USING (auth.uid() = user_id);

NOTIFY pgrst, 'reload schema';
```

Aplicar con: Cowork puede ejecutar via MCP execute_sql (más confiable que `db push` que tuvo bug con apply_migration), o Enrique con `npx supabase db push` post-merge.

### 3.B — Ruta `/agenda` con mini-cards horizontales

Archivo nuevo: `app/agenda.tsx`

**Layout:**
```
Header: "AGENDA DE HOY" + fecha + chip "X eventos · Y próximos"
[scroll vertical de mini-cards horizontales agrupadas por hora]
[Botón flotante "+"] → modal agregar evento custom
```

**Mini-card horizontal componente nuevo:** `src/components/agenda/AgendaMiniCard.tsx`

```tsx
interface Props {
  event: AgendaEventInstance;  // { id, name, time, category, status, scheduledAt }
  onTap?: () => void;          // abre modal de acción
}

// Layout horizontal: imagen thumbnail (60×60 B/N rounded) + texto + hora derecha
// Estados visuales:
//   - pending: opacidad full
//   - completed: tachado + lima check
//   - skipped: gris translúcido
//   - snoozed: clock icon + "+X min"
```

Imagen: `pickAgendaImage(category, seed)` ya existe en `agenda-image-picker.ts` — reusar.

### 3.C — Acceso desde HOY (sentirse vinculada)

Archivo: `app/(tabs)/index.tsx` o `src/components/economy/HoyDayCardEditorial.tsx`

Agregar bloque ENTRE `TU DÍA editorial` y la primera sub-sección `DESPERTAR`:

```tsx
<Pressable onPress={() => router.push('/agenda')} style={styles.agendaPreview}>
  <View style={styles.agendaHeader}>
    <EliteText style={styles.title}>AGENDA DE HOY</EliteText>
    <EliteText style={styles.chip}>{nextCount} próximos →</EliteText>
  </View>
  {/* Mini-preview: próximos 3 eventos como mini-cards compactas */}
  {nextThreeEvents.map(ev => (
    <AgendaMiniCard key={ev.id} event={ev} compact />
  ))}
</Pressable>
```

Estilo: card editorial con borde sutil + tap navega a /agenda. Si no hay eventos próximos: estado vacío con CTA "Configura tu agenda".

### 3.D — Auto-generación + override manual

Extender `src/services/protocol-builder-service.ts` o crear `src/services/agenda-service.ts`:

**`generateAgendaEvents(userId, date)`:**
1. Lee `user_protocols` activos → templates → default_actions
2. Lee `user_chronotype.schedule` → wake/sleep/meals
3. Genera eventos en `agenda_events` con `source='protocol'` o `source='chronotype'`
4. Idempotente: si ya hay eventos para ese día con esos sources, no duplicar
5. Crea instancias en `agenda_event_logs` para hoy

**`updateAgendaEvent(eventId, changes)`:**
- Permite al usuario editar nombre, hora, notify_minutes_before
- Si edita evento `source='protocol'`, marca `source='manual_override'` para no sobrescribirlo en próximas auto-gens

**`deleteAgendaEvent(eventId)`:**
- Soft delete: `is_active = false`
- Si era de protocolo, el usuario lo "quitó de su agenda" — auto-gen futura NO lo recrea (registrar en `user_day_preferences.disabled_protocol_events`)

**`createCustomEvent(userId, name, time, category, notifyMin)`:**
- INSERT con `source='manual'`

### 3.E — Editor inline en `/agenda` (reusar EditDayModal)

Componente existente: `EditDayModal.tsx`

Adaptar para flujo agenda:
- Tap sobre mini-card → `<EventActionModal>` con 4 botones:
  - **Editar** → abre `EditEventModal` (nombre, hora, category dropdown, notify_minutes_before slider)
  - **Completar manual** → marca log como `completed` + emite `day_changed`
  - **Posponer** (+15/+30/+60) → actualiza scheduled_at del log
  - **Eliminar** → confirm dialog → soft delete

Tap "+" floating → `CreateEventModal` (formulario plano: nombre, hora, category, notify_minutes_before).

Mantener UI plana — input fields + buttons. NO drag-drop, NO swipe gestures.

### 3.F — Push notifications server-side

**Step 1: token registration en client**

Archivo nuevo: `src/services/push-notification-service.ts`

```typescript
import * as Notifications from 'expo-notifications';
import * as Device from 'expo-device';
import { Platform } from 'react-native';
import { supabase } from '@/src/lib/supabase';

export async function registerForPushNotificationsAsync(userId: string) {
  if (!Device.isDevice) return null;
  
  const { status: existing } = await Notifications.getPermissionsAsync();
  let finalStatus = existing;
  if (existing !== 'granted') {
    const { status } = await Notifications.requestPermissionsAsync();
    finalStatus = status;
  }
  if (finalStatus !== 'granted') return null;
  
  const tokenData = await Notifications.getExpoPushTokenAsync({
    projectId: Constants.expoConfig?.extra?.eas?.projectId,
  });
  
  if (Platform.OS === 'android') {
    await Notifications.setNotificationChannelAsync('default', {
      name: 'default',
      importance: Notifications.AndroidImportance.DEFAULT,
    });
  }
  
  // Upsert token (puede haber múltiples devices por user)
  await supabase.from('user_notification_tokens').upsert({
    user_id: userId,
    expo_push_token: tokenData.data,
    platform: Platform.OS,
    last_used_at: new Date().toISOString(),
  }, { onConflict: 'user_id,expo_push_token' });
  
  return tokenData.data;
}
```

Llamar en `auth-context` post-login (1 vez por sesión).

**Step 2: Edge Function que dispara push**

Archivo nuevo: `supabase/functions/dispatch-agenda-notifications/index.ts`

```typescript
import { serve } from "https://deno.land/std@0.168.0/http/server.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

const SUPABASE_URL = Deno.env.get("SUPABASE_URL")!;
const SUPABASE_SERVICE_ROLE = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!;

serve(async (req) => {
  const supabase = createClient(SUPABASE_URL, SUPABASE_SERVICE_ROLE);
  const now = new Date().toISOString();
  
  // 1) Buscar logs con notify_at <= now AND notified_at IS NULL
  const { data: pending } = await supabase
    .from('agenda_event_logs')
    .select('id, user_id, scheduled_at, event_id, agenda_events(name, category)')
    .lte('notify_at', now)
    .is('notified_at', null)
    .limit(200);
  
  if (!pending?.length) return new Response('No pending', { status: 200 });
  
  // 2) Para cada user, obtener su(s) token(s)
  const userIds = [...new Set(pending.map(p => p.user_id))];
  const { data: tokens } = await supabase
    .from('user_notification_tokens')
    .select('user_id, expo_push_token')
    .in('user_id', userIds);
  
  const tokenMap = new Map<string, string[]>();
  for (const t of tokens ?? []) {
    if (!tokenMap.has(t.user_id)) tokenMap.set(t.user_id, []);
    tokenMap.get(t.user_id)!.push(t.expo_push_token);
  }
  
  // 3) Construir messages para expo push API
  const messages = [];
  for (const log of pending) {
    const userTokens = tokenMap.get(log.user_id) ?? [];
    const event = log.agenda_events as any;
    for (const token of userTokens) {
      messages.push({
        to: token,
        title: 'ATP — Próximo evento',
        body: `${event?.name ?? 'Evento'} · en breve`,
        data: { logId: log.id, eventId: log.event_id, category: event?.category },
        sound: 'default',
      });
    }
  }
  
  // 4) Enviar a Expo push API (batches de 100)
  for (let i = 0; i < messages.length; i += 100) {
    const batch = messages.slice(i, i + 100);
    await fetch('https://exp.host/--/api/v2/push/send', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(batch),
    });
  }
  
  // 5) Marcar logs como notified
  const logIds = pending.map(p => p.id);
  await supabase
    .from('agenda_event_logs')
    .update({ notified_at: now })
    .in('id', logIds);
  
  return new Response(`Sent ${messages.length}`, { status: 200 });
});
```

Deploy con `supabase functions deploy dispatch-agenda-notifications`.

**Step 3: Cron job que invoca la Edge Function cada 1 minuto**

Opciones:
- A) **pg_cron** en Supabase (preferido, integrado):
```sql
SELECT cron.schedule(
  'dispatch-agenda-notifications-minutely',
  '* * * * *',
  $$ SELECT net.http_post(
    url := 'https://itqkfozqvpwikogggqng.supabase.co/functions/v1/dispatch-agenda-notifications',
    headers := '{"Authorization": "Bearer <SERVICE_ROLE_KEY>"}'::jsonb
  ); $$
);
```

- B) GitHub Action cron (alternativa si pg_cron no disponible).

**Recomendación: A (pg_cron)** — la extensión está activa en Supabase moderno.

**Step 4: Cuando se crea/edita un evento en /agenda, calcular `notify_at`**

En `agenda-service.ts`, al crear/editar un log:

```typescript
const notifyAt = event.notify_minutes_before > 0
  ? new Date(scheduledAt.getTime() - event.notify_minutes_before * 60000)
  : null;

await supabase.from('agenda_event_logs').upsert({
  user_id, event_id, date,
  scheduled_at: scheduledAt.toISOString(),
  notify_at: notifyAt?.toISOString() ?? null,
  status: 'pending',
}, { onConflict: 'user_id,event_id,date' });
```

### 3.G — Sync HOY ↔ Agenda

- Cuando user completa evento en /agenda → `agenda_event_logs.status='completed'` + emit `day_changed`
- Cuando user completa electrón en HOY (ej. tap LUZ SOLAR) → si hay evento en agenda hoy con category matching, marcar log como `completed`

### 3.H — Solicitud de permisos de notificación

En onboarding (al final, paso opcional): "ATP te recuerda tus eventos del día. ¿Permites notificaciones?"

Si user dice no → toast: "Puedes activarlas después en Settings". No bloquear flow.

Si user dice sí → `registerForPushNotificationsAsync(userId)`.

Si ya pasó onboarding y nunca dio permiso → al primer tap en "Editar notify" de un evento, prompt inline.

---

## 4. Criterios de aceptación

1. `npx tsc --noEmit` cero errores
2. `vitest run` todos los tests pasan
3. **Migración 098 aplicada al remoto** (3 tablas + indices + RLS + 1 NOTIFY)
4. **Ruta `/agenda`** accesible desde HOY (botón visible entre TU DÍA y DESPERTAR)
5. **Mini-cards horizontales** muestran imagen B/N por categoría + nombre + hora + estado visual
6. **Editor:** tap card → modal con 4 acciones (editar/completar/posponer/eliminar) — todas funcionan + persisten
7. **Crear evento custom:** botón "+" → modal → save → aparece en agenda + persiste
8. **Auto-generación:** primer load del día genera eventos desde protocolo + cronotipo en `agenda_events` table
9. **Override manual:** editar evento de protocolo → próximas auto-gens NO lo sobreescriben
10. **Token registration:** post-login, registra expo push token en `user_notification_tokens`
11. **Edge Function `dispatch-agenda-notifications` deployada** y funcional
12. **Cron pg_cron** registrado para invocar la Edge Function cada minuto
13. **Notif real llega al device** cuando un evento tiene notify_minutes_before configurado y se cumple el momento
14. **Sync:** completar electrón en HOY que matchea categoría de evento → evento en agenda se marca completed

---

## 5. Smoke test (Enrique post-OTA + post-Edge deploy)

- [ ] HOY: arriba aparece bloque "AGENDA DE HOY · X próximos →"
- [ ] Tap → navega a /agenda
- [ ] /agenda muestra timeline con mini-cards horizontales por evento
- [ ] Tap card → modal con Editar / Completar / Posponer / Eliminar
- [ ] Editar evento → cambia nombre/hora → persiste y se ve actualizado
- [ ] Tap "+" → crear evento custom (nombre + hora + category + notify_min)
- [ ] Configurar notify_minutes_before=2 en un evento → esperar 2 min → llega push al device
- [ ] Atrás regresa a HOY (no a otra tab)
- [ ] Permisos notif: al primer evento con notify_min → prompt aparece (si no se dio en onboarding)

---

## 6. Deploy

```powershell
cd D:\Proyectos_ClaudeCode\ELITE_Timer\EliteTimer
git checkout main
git pull origin main
git checkout -b feat/agenda-v13g
# ... cambios por fase (3.A SQL → 3.B-C frontend → 3.D-E lógica → 3.F push) ...
npx tsc --noEmit
git add .
git commit -m "feat(v13g): AGENDA completa — backend + ruta + edición + push server-side"
git push origin feat/agenda-v13g

# Aplicar migración 098 al remoto:
# Opción A: Cowork via MCP execute_sql (más confiable)
# Opción B: npx supabase db push (puede fallar como antes)

# Deploy Edge Function:
supabase functions deploy dispatch-agenda-notifications

# Configurar pg_cron (vía SQL Editor o MCP execute_sql)

# Audit del branch + merge
# OTA: eas update --branch preview --message "feat(v13g): AGENDA completa"
```

---

## 7. Notas no negociables

- str_replace quirúrgico, no reescribir
- TypeScript clean
- Migración 098 idempotente (CREATE IF NOT EXISTS, ON CONFLICT DO NOTHING)
- RLS en las 3 tablas nuevas (auth.uid() = user_id)
- Token push: usar `Constants.expoConfig.extra.eas.projectId` (no hardcoded)
- Edge Function: usar `SUPABASE_SERVICE_ROLE_KEY` para acceder data sin RLS (server-side trusted)
- Defensivo: si tabla agenda_events está vacía, /agenda muestra estado vacío con CTA
- Reusar componentes/services existentes: EditDayModal, generateDailyPlan, pickAgendaImage, custom_agenda_actions
- Cero imágenes nuevas necesarias (las 45 de agenda/ ya cubren las 12 categorías)
- Sleep queda informativo en HOY (sin tocar)
- HeroAgendaCard.tsx queda dead code (limpieza en follow-up #26)

---

## 8. Decisiones de criterio ya cerradas

- **Ubicación:** ruta dedicada `/agenda` accesible desde HOY (botón con preview de próximos 3)
- **Generación:** auto-gen + usuario sobreescribe (marca como manual_override)
- **Notifs:** push server-side completo (Edge Function + tokens + pg_cron + remote scheduling)
- **Visual:** mini-cards horizontales con imagen B/N thumbnail (45 imgs `agenda/*` ya generadas)
- **Editor:** reusa EditDayModal pattern, formulario plano (no drag-drop)
- **Permisos notif:** prompt en onboarding (paso opcional) + fallback al primer evento con notify_min
- **Widgets:** NO en este sprint (V2.x+, pero data layer compatible para futuro)

---

## 9. Phases internas (CC puede comitear por fase)

| Fase | Sección | Tiempo estimado |
|---|---|---|
| F1 | 3.A migración 098 SQL + aplicar | 30 min |
| F2 | 3.B componente AgendaMiniCard | 1h |
| F3 | 3.C acceso desde HOY | 30 min |
| F4 | 3.D agenda-service auto-gen + override | 2h |
| F5 | 3.E editor inline + crear custom | 2h |
| F6 | 3.F push notifications full (token + Edge + cron) | 4h |
| F7 | 3.G sync HOY↔Agenda + 3.H permisos onboarding | 1h |
| **TOTAL** | | **~11h** |

Si CC no puede terminar todo en una vuelta: split natural es F1-F5 (Sprint G1 — agenda funcional) + F6-F7 (Sprint G2 — push real).

---

**FIN BUZÓN** — Claude Code: 8 secciones, 7 fases. Commits granulares por fase. Foco: agenda funcional + push server-side + sentirse vinculada a HOY. Cierre de la primera pantalla del flujo macro retomado post-rediseño.
